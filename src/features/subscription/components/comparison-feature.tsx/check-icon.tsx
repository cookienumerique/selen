import { Colors } from "@/src/constants/theme"
import { Ionicons } from "@expo/vector-icons"

export const CheckIcon = () => {
    return <Ionicons name="checkmark" size={24} color={Colors.warmSand} />
}